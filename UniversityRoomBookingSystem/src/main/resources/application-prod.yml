spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mysql://localhost:3306/room_booking
    username: root
    password: ${DB_PASSWORD}   # read password from environment variable

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

server:
  port: 8080
